{
  "name": "resume",
  "version": "1.0.0",
  "description": "",
  "main": "test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -f resume.pdf && rm -f resume.pdf.html && rm -f resume.html",
    "build": "resume export resume.html -t ./node_modules/jsonresume-theme-elegant",
    "build-pdf": "resume export resume.pdf -t jsonresume-theme-elegant && open resume.pdf",
    "build-alt": "resume export resume.pdf -t caffeine"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonresume-theme-caffeine": "^1.2.3",
    "jsonresume-theme-elegant": "^1.16.1",
    "jsonresume-theme-flat": "^0.3.7",
    "jsonresume-theme-smart": "^1.0.2"
  }
}
